<div class="row">
	<div class="page-header">
		<h2>New Field <small>for <%= @project.title %></small></h2>
	</div>
	<%= simple_form_for [@project,@field] do |f| %>
		<%= render "form", f:f %>
		<%= f.button :submit, "Create Field", class:"btn btn-primary" %>
	<% end %>
</div>